language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
install:
  - pip install flit
  - flit install
script:
  - pytest --black --mypy --mypy-ignore-missing-imports --cov=pms --cov-report=term-missing --cov-fail-under=70